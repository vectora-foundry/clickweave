name = "Conditional screenshot"

[[turns]]
prompt = "Open Calculator, click 5, click +, click 3, click =. If the result shows 8, take a screenshot. Otherwise click Clear."
[turns.expect]
min_nodes = 6
max_nodes = 10
required_tools = ["click"]
required_patterns = ["conditional"]
